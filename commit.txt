1. Backend Error Messages (in Controllers)

  - Added custom sendFailedLoginResponse() method to all login controllers
  - Shows clear error: "Invalid username/email or password. Please check your
  credentials and try again."
  - Added custom validation messages for empty fields

  2. Frontend Inline Error Display (in Views)

  Added visible error alerts to all login pages that show:
  - Validation errors (empty fields, etc.)
  - Login failure errors (wrong password/username)
  - Session flash messages

  The errors now appear as:
  - Bootstrap alert boxes at the top of the login form (red error boxes)
  - Field-level validation with red borders and error text under each field
  - Toast notifications (the existing iziToast system)

  3. Files Modified:

  - Admin/Auth/LoginController.php (Lines 99-105)
  - Manager/Auth/LoginController.php (Lines 104-110)
  - Staff/Auth/LoginController.php (Lines 103-109)
  - Customer/Auth/LoginController.php (Lines 35-66)
  - All 4 login blade views with inline error displays